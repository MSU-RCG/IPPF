Given /^an "([^"]*)" is pending$/ do |arg1|
  pending # express the regexp above with the code you wish you had
end

When /^the job is being processed$/ do
  pending # express the regexp above with the code you wish you had
end

Then /^a background worker will check periodically for the completed files in a "([^"]*)" subdirectory$/ do |arg1|
  pending # express the regexp above with the code you wish you had
end

Given /^the computation on the job is complete$/ do
  pending # express the regexp above with the code you wish you had
end

Then /^a background worker will download the file to the server$/ do
  pending # express the regexp above with the code you wish you had
end

Then /^I should see a "([^"]*)" link for the computed job file$/ do |arg1|
  pending # express the regexp above with the code you wish you had
end